<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/buttonDay"
            android:layout_width="221dp"
            android:layout_height="24dp"
            android:gravity="center"
            android:background="@drawable/custom_background_day"
            app:buttonDay="day"
            android:text="@string/button_day"
            android:textColor="@color/white"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/buttonHour"
            android:layout_width="220dp"
            android:layout_height="24dp"
            android:background="@drawable/custom_background_hour"
            android:gravity="center"
            android:text="@string/button_hour"
            android:textColor="@color/white"
            app:layout_constraintBottom_toBottomOf="@+id/buttonDay"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/buttonDay"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</merge>